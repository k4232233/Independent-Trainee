# MySql数据库配置
## 服务端口号
#server:
#  port: 8080
#
## 数据库地址
#datasource:
#  url: localhost:3306/blog_test
#
#spring:
#  datasource: # 数据库配置
#    driver-class-name: com.mysql.jdbc.Driver
#    url: jdbc:mysql://${datasource.url}?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
#    username: root
#    password: 123456
#    hikari:
#      maximum-pool-size: 10 # 最大连接池数
#      max-lifetime: 1770000
#
#mybatis:
#  # 指定别名设置的包为所有entity
#  type-aliases-package: com.itcodai.course10.entity
#  configuration:
#    map-underscore-to-camel-case: true # 驼峰命名规范
#  mapper-locations: # mapper映射文件位置
#    - classpath:mapper/*.xml

# H2数据库配置
server:
  port: 8888 # 端口号

spring:
  datasource:
    url: jdbc:h2:~/mydb  # 数据库
#    url: jdbc:h2:file:~/mydb #文件模式：把数据持久化到文件中
    driver-class-name: org.h2.Driver
    username: root
    password: test
  h2:
    console:
      path: /h2-console  #h2嵌入式数据库控制台，可以通过浏览器访问
      enabled: true

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开启sql日志
    map-underscore-to-case: true # 开启驼峰映射(mp默认开启)


mybatis:
  # 指定别名设置的包为所有entity
  type-aliases-package: com.sbi.entity
  configuration:
    map-underscore-to-camel-case: true # 驼峰命名规范
  mapper-locations: # mapper映射文件位置
    - classpath:mapper/*.xml
